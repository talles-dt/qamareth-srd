# Cloudflare Pages serves Astro's directory output from each route's index.html.
# Do not redirect section wildcards to `:splat/index.html`: a request for
# `/lore/o-mundo/index.html` also matches `/lore/*`, causing recursive redirects.
